#include<iostream>
#include<vector>
#include<string>
#include<stack>
#include <sstream>

using namespace std;

void TwoQueue();

int main()
{

}

void TwoQueue()
{
	auto Q = 0;
	cin >> Q;

	for (auto i = 0; i < Q; ++i)
	{
		string s;
		getline(cin, s);
		stringstream ss(s);
		vector<int> query;

		while (getline(ss, s, ' '))
		{
			query.emplace_back()
		}
	}
}